const charactersBaseUrl = 'https://kitsu.io/api/edge/';

const charactersEndpoint = 'characters';

const searchCharactersNameEndpoint = '?filter[name]=';

const quotesBaseUrl = 'https://api.quotable.io/random?tags=';

const quoteTagEndpoint = 'wisdom';

// https://kitsu.io/api/edge/characters?filter[name]=value